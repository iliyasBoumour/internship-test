import { styled } from "@mui/system";
import {
  Card,
  Container,
  Toolbar,
  Typography,
  Link,
  AppBar,
} from "@mui/material";

const MyContainer = styled(Container)`
  /* padding: theme.spacing(2); */
`;
