import { styled } from "@mui/system";
import {
  Card,
  Container,
  Toolbar,
  Typography,
  Link,
  AppBar,
} from "@mui/material";

export const SimpleLink = styled(Link)`
  text-decoration: none;
  color: inherit;
`;
